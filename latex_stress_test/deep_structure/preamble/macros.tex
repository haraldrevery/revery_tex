% Deliberately a chain: macros.tex inputs commands.tex, which inputs
% lengths.tex. A reader that only walks one level finds two thirds of it.
\newcommand{\stressterm}[1]{\textbf{#1}}
\input{preamble/commands}
