% A space in the filename. LaTeX needs the braces; the file panel, the
% include-graph walk and every backend path check have to cope too.
\subsection{A file whose name contains spaces}
This text comes from \texttt{modules/notes with spaces.tex}.
