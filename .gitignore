# BusyTeX engine assets: 649 MB, individual files up to 324 MB.
# GitHub hard-rejects files over 100 MB, so these are never committed.
# Fetch with:  npx texlyre-busytex download-assets www/engine
# or:          curl -L https://github.com/TeXlyre/texlyre-busytex/releases/download/assets-v1.2.3/busytex-assets.tar.gz | tar xz -C www/engine
www/engine/busytex/
www/engine/*.tar.gz

# Phase 0 compile logs (regenerated by test/run_phase0.js)
build_tools/phase0_logs/

node_modules/
*.log
.DS_Store
